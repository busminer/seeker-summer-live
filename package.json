{
  "name": "seeker-summer-live",
  "version": "1.0.0",
  "private": true,
  "description": "A live animated community visualization of real SKR claims during Seeker Summer",
  "scripts": {
    "dev": "node server.js",
    "check": "node --check app.js && node --check api/state.js && node --check api/health.js && node --check server.js"
  },
  "engines": { "node": ">=20" }
}
